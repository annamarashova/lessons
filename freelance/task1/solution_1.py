def source_to_destination_text(source_text: str) -> str:
    # TODO: реализовать функцию, которая преобразует текст сообщения из формата source channel в формат destination channel
    return source_text
